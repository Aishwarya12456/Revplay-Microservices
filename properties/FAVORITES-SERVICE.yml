server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/favorites_db
    username: root
    password: Aishu@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.springframework.security: DEBUG
    com.revplay: DEBUG