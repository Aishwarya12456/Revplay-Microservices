spring:
  application:
    name: PLAYLIST-SERVICE

  config:
    import: optional:configserver:http://localhost:8888

  profiles:
    active: local

  datasource:
    username: root
    password: Aishu@123
    url: jdbc:mysql://localhost:3306/revplay_playlist_db?createDatabaseIfNotExist=true&zeroDateTimeBehavior=convertToNull
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 10
